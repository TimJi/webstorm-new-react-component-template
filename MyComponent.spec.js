/* eslint-disable no-unused-expressions */
// import React from 'react';
import { expect } from 'chai';
import MyComponent  from './index';

describe('FormModal', function MyComponentSpec () {
    it('it exists', () => {
        expect(MyComponent).to.be.ok;
    });
});
